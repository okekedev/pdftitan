name: Build and Deploy to Azure Container Apps

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
      id-token: write  # Required for OIDC
    
    steps:
    - name: Checkout code
      uses: actions/checkout@v4
      
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    # ✅ Standard approach: GITHUB_TOKEN for build/push
    - name: Login to GitHub Container Registry
      uses: docker/login-action@v3
      with:
        registry: ghcr.io
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}
        
    - name: Create short SHA
      id: short_sha
      run: echo "sha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT
      
    - name: Extract metadata for Docker
      id: meta
      uses: docker/metadata-action@v5
      with:
        # ✅ Dynamic repository reference for PDFTitan
        images: ghcr.io/${{ github.repository_owner }}/pdftitan
        tags: |
          type=raw,value=latest,enable=${{ github.ref == format('refs/heads/{0}', 'main') }}
          type=raw,value=v${{ github.run_number }},enable=${{ github.ref == format('refs/heads/{0}', 'main') }}
          type=raw,value=${{ steps.short_sha.outputs.sha }},enable=${{ github.ref == format('refs/heads/{0}', 'main') }}
          
    - name: Build and push Docker image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: true
        tags: ${{ steps.meta.outputs.tags }}
        labels: ${{ steps.meta.outputs.labels }}
        cache-from: type=gha
        cache-to: type=gha,mode=max

    # Deploy only on main branch
    - name: Azure Login with OIDC
      if: github.ref == 'refs/heads/main'
      uses: azure/login@v2
      with:
        client-id: ${{ secrets.AZURE_CLIENT_ID }}
        tenant-id: ${{ secrets.AZURE_TENANT_ID }}
        subscription-id: ${{ secrets.AZURE_SUBSCRIPTION_ID }}
        
    - name: Deploy to Azure Container Apps - PDFTitan with ServiceTitan Environment Variables
      if: github.ref == 'refs/heads/main'
      run: |
        echo "🚀 Deploying PDFTitan to Azure Container Apps with ServiceTitan integration..."
        
        # Create unique revision suffix with date and short commit
        REVISION_SUFFIX=$(date +%Y%m%d-%H%M%S)-${{ steps.short_sha.outputs.sha }}
        echo "📋 Creating revision: $REVISION_SUFFIX"
        
        # ✅ Deploy with ServiceTitan environment variables
        az containerapp update \
          --name pdftitan-app \
          --resource-group pdftitan-rg \
          --image ghcr.io/${{ github.repository_owner }}/pdftitan:latest \
          --revision-suffix $REVISION_SUFFIX \
          --set-env-vars \
            "NODE_ENV=production" \
            "PORT=3004" \
            "REACT_APP_SERVICETITAN_CLIENT_ID=${{ secrets.REACT_APP_SERVICETITAN_CLIENT_ID }}" \
            "REACT_APP_SERVICETITAN_CLIENT_SECRET=${{ secrets.REACT_APP_SERVICETITAN_CLIENT_SECRET }}" \
            "REACT_APP_SERVICETITAN_APP_KEY=${{ secrets.REACT_APP_SERVICETITAN_APP_KEY }}" \
            "REACT_APP_SERVICETITAN_TENANT_ID=${{ secrets.REACT_APP_SERVICETITAN_TENANT_ID }}" \
            "REACT_APP_SERVICETITAN_AUTH_URL=${{ secrets.REACT_APP_SERVICETITAN_AUTH_URL }}" \
            "REACT_APP_SERVICETITAN_API_BASE_URL=${{ secrets.REACT_APP_SERVICETITAN_API_BASE_URL }}"
        
        echo "✅ Deployment completed successfully with ServiceTitan configuration!"
        
    - name: Verify deployment and check app health
      if: github.ref == 'refs/heads/main'
      run: |
        echo "🔍 Verifying deployment status..."
        sleep 30
      
        az containerapp revision list \
          --name pdftitan-app \
          --resource-group pdftitan-rg \
          --query '[0].{Name:name,Active:properties.active,CreatedTime:properties.createdTime,Image:properties.template.containers[0].image,TrafficWeight:properties.trafficWeight}' \
          --output table
        
        APP_URL=$(az containerapp show \
          --name pdftitan-app \
          --resource-group pdftitan-rg \
          --query 'properties.configuration.ingress.fqdn' \
          --output tsv)
        
        if [ ! -z "$APP_URL" ]; then
          echo "🌐 Application available at: https://$APP_URL"
          
          # Test health endpoint
          if curl -f -s --max-time 30 "https://$APP_URL/health" > /dev/null; then
            echo "✅ Application is responding successfully!"
            
            # Test ServiceTitan configuration
            HEALTH_RESPONSE=$(curl -s "https://$APP_URL/health" | jq -r '.serviceIntegration.configured // false')
            if [ "$HEALTH_RESPONSE" = "true" ]; then
              echo "✅ ServiceTitan integration is properly configured!"
            else
              echo "⚠️ ServiceTitan integration may not be fully configured. Check environment variables."
            fi
          else
            echo "⚠️ Application may still be starting up. Check logs in Azure portal."
          fi
        fi
        
        echo "📊 Deployment summary:"
        echo "- Image: ghcr.io/${{ github.repository_owner }}/pdftitan:latest"
        echo "- Build: v${{ github.run_number }}"
        echo "- Commit: ${{ steps.short_sha.outputs.sha }}"
        echo "- Registry: GitHub Container Registry (ghcr.io)"
        echo "- ServiceTitan: Environment variables configured"
        echo "- Status: ✅ Deployed"